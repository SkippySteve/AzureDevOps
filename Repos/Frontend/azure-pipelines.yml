trigger:
- main

variables:
  acrName: 'acrxs344ph3injno'
  azureServiceConnection: 'pipeline-deploy-to-prod'

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: AzureCLI@2
  displayName: Login to ACR
  inputs:
    azureSubscription: $(azureServiceConnection)
    scriptType: bash
    scriptLocation: inlineScript
    inlineScript: |
      az acr login --name $(acrName)

- script: |
    IMAGE_TAG=$(Build.BuildId)
    echo "##vso[task.setvariable variable=imageTag]$IMAGE_TAG"
    echo $IMAGE_TAG > frontend-image-tag.txt
    docker build -t $(acrName).azurecr.io/capstone-frontend:$IMAGE_TAG .
    docker push $(acrName).azurecr.io/capstone-frontend:$IMAGE_TAG
  displayName: Build & Push Frontend

- publish: frontend-image-tag.txt
  artifact: frontend-image-tag
